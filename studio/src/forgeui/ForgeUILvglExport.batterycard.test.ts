import{generateForgeUILvglCode}from'./ForgeUILvglExport';const card=(id:string):IComponent=>({id,componentName:id,type:'BatteryCard',parent:'root',children:[],props:{x:10,y:10,w:380,h:300,simulationMode:'discharging',generateRuntimeApi:true,enableUserEvents:true}});const generate=(...items:IComponent[])=>generateForgeUILvglCode({root:{id:'root',parent:'root',type:'Box',props:{},children:items.map(i=>i.id)},...Object.fromEntries(items.map(i=>[i.id,i]))},'graphite',undefined,{includeThemeTexture:false});describe('Battery Card export',()=>{it('emits stable enum, semantic silent APIs and selected event',()=>{const o=generate(card('House'));expect(o.code).toContain('FG_BATTERY_STATE_DISCHARGING');expect(o.code).toContain('void FG_Set_House_Battery_State_Of_Charge(float percent)');expect(o.publicApiDeclarations).toEqual(expect.arrayContaining(['void FG_Set_House_Battery_State(fg_battery_state_t state);','void FG_Set_House_Battery_Electrical(float voltage, float current, float power);']));expect(o.userEventHooks).toContain('FG_On_House_Battery_Selected');const setter=o.code.slice(o.code.indexOf('void FG_Set_House_Battery_State_Of_Charge'),o.code.indexOf('void FG_Set_House_Battery_State('));expect(setter).not.toContain('FG_On_')});it('isolates duplicates',()=>{const c=generate(card('Port'),card('Starboard')).code;expect(c).toContain('fg_port_battery_data');expect(c).toContain('fg_starboard_battery_data')})})
