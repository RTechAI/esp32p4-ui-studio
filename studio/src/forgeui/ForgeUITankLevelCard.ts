export const FORGEUI_TANK_LEVEL_CARD_SCHEMA_VERSION=1
export type ForgeUITankState='unknown'|'empty'|'filling'|'draining'|'stable'|'full'|'low'|'critical'|'overflow'|'fault'|'disconnected'
const num=(v:unknown,f:number,min=-Number.MAX_VALUE,max=Number.MAX_VALUE)=>{const n=Number(v);return Math.max(min,Math.min(max,Number.isFinite(n)?n:f))};const bool=(v:unknown,f:boolean)=>typeof v==='boolean'?v:f;const text=(v:unknown,f='',max=64)=>(typeof v==='string'?v.trim():f).slice(0,max);const pick=<T extends string>(v:unknown,a:readonly T[],f:T)=>a.includes(v as T)?v as T:f
export const normalizeForgeUITankLevelCard=(p:Record<string,unknown>={})=>{const critical=num(p.criticalThreshold,10,0,100),low=Math.max(critical,num(p.lowThreshold,20,0,100)),full=Math.max(low,num(p.fullThreshold,95,0,100)),overflow=Math.max(full,num(p.overflowThreshold,100,0,100));return{schemaVersion:1 as 1,title:text(p.title,'Fresh Water',64)||'Fresh Water',tankType:pick(p.tankType,['fresh-water','grey-water','black-water','diesel','petrol','oil','hydraulic','coolant','lpg','chemical','waste','hopper','silo','generic']as const,'fresh-water'),displayMode:pick(p.displayMode,['compact','detailed','industrial']as const,'detailed'),tankOrientation:pick(p.tankOrientation,['vertical','horizontal','rectangular']as const,'vertical'),percentage:num(p.percentage,72,0,100),volume:num(p.volume,360,0),capacity:num(p.capacity,500,0),level:num(p.level,1.44,0),depth:num(p.depth,2,0),flowRate:num(p.flowRate,-8.5),estimatedFillTime:num(p.estimatedFillTime,0,0),estimatedEmptyTime:num(p.estimatedEmptyTime,42,0),temperature:num(p.temperature,18.5),density:num(p.density,0,0),state:pick(p.state,['unknown','empty','filling','draining','stable','full','low','critical','overflow','fault','disconnected']as const,'draining'),statusText:text(p.statusText,'Pump running',64),lowThreshold:low,criticalThreshold:critical,fullThreshold:full,overflowThreshold:overflow,showTankGraphic:bool(p.showTankGraphic,true),showPercentage:bool(p.showPercentage,true),showVolume:bool(p.showVolume,true),showCapacity:bool(p.showCapacity,true),showLevel:bool(p.showLevel,false),showFlow:bool(p.showFlow,true),showRuntimeEstimate:bool(p.showRuntimeEstimate,true),showTemperature:bool(p.showTemperature,false),showStatusText:bool(p.showStatusText,true),fillColour:text(p.fillColour,'#38BDF8',7),border:bool(p.border,true),rounded:bool(p.rounded,true),shadow:bool(p.shadow,true),glassStyle:bool(p.glassStyle,false),simulationMode:text(p.simulationMode,'marine-water',24),generateRuntimeApi:bool(p.generateRuntimeApi,true),enableUserEvents:bool(p.enableUserEvents,true)}}
export type ForgeUITankLevelCardModel=ReturnType<typeof normalizeForgeUITankLevelCard>
export const simulateForgeUITank=(m:ForgeUITankLevelCardModel)=>{const x:any={empty:{state:'empty',percentage:0,flowRate:0},filling:{state:'filling',percentage:46,flowRate:12.5},full:{state:'full',percentage:98,flowRate:0},draining:{state:'draining',percentage:72,flowRate:-8.5},overflow:{state:'overflow',percentage:100,flowRate:4.2},fault:{state:'fault',percentage:42,flowRate:0},'marine-water':{state:'stable',percentage:72},diesel:{state:'draining',percentage:58,flowRate:-4.3},chemical:{state:'stable',percentage:64,flowRate:0}};return{...m,...(x[m.simulationMode]||{})}as ForgeUITankLevelCardModel}
