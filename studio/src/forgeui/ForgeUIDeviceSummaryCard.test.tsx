import React from'react';import{ChakraProvider}from'@chakra-ui/react';import{render,screen}from'@testing-library/react';import{formatForgeUIDeviceBytes,formatForgeUIDeviceUptime,normalizeForgeUIDeviceSummaryCard,simulateForgeUIDeviceSummary}from'./ForgeUIDeviceSummaryCard';import{ForgeUIDeviceSummaryCardPreview}from'./preview/ForgeUIDeviceSummaryCardPreview';import{FG_PREVIEW_PALETTES}from'./preview/forgeThemeMap';import{getForgeUIWidgetDefinition}from'./widgets/ForgeUIWidgetRegistry'
describe('Device Summary Card',()=>{it('normalizes semantic defaults and bounds',()=>{expect(normalizeForgeUIDeviceSummaryCard({health:'bad',batteryPercentage:120,signalQuality:-1})).toMatchObject({health:'healthy',batteryPercentage:100,signalQuality:0})});it('formats uptime and storage deterministically',()=>{expect(formatForgeUIDeviceUptime(388800)).toBe('4d 12h 00m');expect(formatForgeUIDeviceBytes(7340032000)).toBe('6.8 GB')});it('simulates deterministically',()=>expect(simulateForgeUIDeviceSummary(normalizeForgeUIDeviceSummaryCard({simulationMode:'storage-fault'}))).toMatchObject({storageAvailable:false,health:'attention'}));it('registers and renders Native Component #11',()=>{expect(getForgeUIWidgetDefinition('DeviceSummaryCard')).toMatchObject({displayName:'Device Summary Card',origin:'forgeui-native',defaultWidth:400});render(<ChakraProvider><ForgeUIDeviceSummaryCardPreview component={{id:'main',type:'DeviceSummaryCard',parent:'root',children:[],props:{simulationMode:'healthy-controller'}}} palette={FG_PREVIEW_PALETTES.graphite}/></ChakraProvider>);expect(screen.getByTestId('forgeui-device-summary-card')).toBeInTheDocument();expect(screen.getByText('4d 12h 00m')).toBeInTheDocument()})})
